# 闲不住 更新日志

## v3.0.1 — 数据层安全修复 + 推送链路修复（正式版）

### 🔧 修复

- 充电推送丢失 bus 参数：pushToAgent 无 bus 必返回 false，助手收不到充电通知；补传 ctx.bus || ctx._bus，充电后助手能正常收到「充了电」通知
- /api/visit 输入边界：performVisit 入口补助手 ID 白名单（isValidAgentId）+ partnerConfig 登记检查，路径穿越/原型污染/未登记 ID 一律拒绝；校验函数抽到 lib/validate.js 供 routes 与 actions 共用一套规则
- recharge/update-narrative/llm-supplement-key 三个写接口补同一套白名单（此前装饰类接口查了、这几个漏了）
- 统一数据写锁：锁从 actions.js 下沉到 data.js，全部写接口（领光粒/充电/装饰/配置/排序/隐藏/刷新）纳入同一把锁，消除旧快照跨异步覆盖竞态；恶作剧路径在 LLM 等待后基于最新快照合并写盘，不再覆盖期间的锁外写入
- saveData 写盘失败不再静默：返回布尔值，API 层检查后回 500，不再谎报 success: true（含 performVisit 内部三处）
- 事件队列 30s 超时真取消：AbortController 穿透到 LLM 请求，超时后内部任务不再后台跑；超时定时器补 clearTimeout
- 恶作剧锁内 LLM 调用限时 10s，避免数据写锁被长时间占用
- Python 探测跨平台：existsSync 查不了 PATH 命令，改为 spawnSync --version 真实探测，Linux 只有 python3 时不再误选不存在的 python
- CI 升级：Node 20 升 24（Node 20 test runner 并发反序列化 bug 致 CI 红），测试命令统一 glob，风铃 Python 回归（PyQt6/xvfb）纳入 CI 自动守门
- 测试夹具命名清理：测试数据中的示例助手 id 替换为中性 helperA/B/C（命名红线）
- README 隐私补充：互动回应/小纸条生成会读助手 memory.md 前 1500 字随请求发送至 LLM 服务商

### 🧪 测试

- 新增回归：助手 ID 白名单（路径穿越/原型污染/未登记拒绝）、callLLM 外部 signal 穿透（abort 立即终止 fetch）、写盘失败回 500、充电推送携带 bus 到达 session:send
- JavaScript 66 项 + 风铃 Python 58 项全部通过

## v3.0.0 — 风铃（悬浮球）（正式版）

### 🎐 风铃悬浮球

- 桌面透明置顶小风铃（108px 短身窄版薄荷瓷铃 + 樱粉短册 SVG + 摆动动画 + 拖拽贴边吸附），点开菜单直接送礼/互动/恶作剧（含关机键），走本地代理与页面同一套业务逻辑
- 自动跟随当前对话：操作始终按最后一条用户消息定位最活跃会话
- 面板改为不抢鼠标的工具窗：先显示缓存、后台刷新，相同动作不重复重建按钮，焦点离开或应用失活自动收起，拖动风铃时弹窗同步跟随
- 半自动启动：打开闲不住页面自动启动，手动关闭后本次运行不再自动弹，顶栏「风铃（悬浮球）」开关随时拉起/收起（删除自动/手动设置面板与 pref 接口）

### 🎵 声音系统

- 真实多铃实录（公有领域 4.8s）+ 1.1s 自然淡出，多个独立播放位置，后声不截断前声尾韵
- 纯标准库 DSP（fengling_dsp.py）：轻/重两档铃声各扩多个变体（音高/音量/激励/泛音随机），基频自动估计，换 wav 资源也能对齐泛音
- 铃舌真实碰撞才响：碰撞切片拉长、衰减放宽（尾音更饱满），撞壁门槛/冷却调优，密集像一阵风扫过
- 音质修复：高通去素材风声、低通去高频丝声（铃声泛音区不变）
- 四档音量（静音/轻声/适中/清亮），音量缩放缓存 WAV 走文件异步播放（修复 winsound 不支持内存播放导致无声的问题），默认静音；声音设置持久记忆，兼容旧版声音开关

### 🌬 动画与交互

- 铃身/短册双阻尼摆 + 非等速微风，悬停改为全局光标轮询可靠触发
- 左右来风与速度感知：从左/右侧移入触发镜像摆动，划入越快阵风越强；宽松热区 + 进出滞回 + 离开宽限，避免边缘反复补阵风
- 柔线铃舌：连接线/铃舌/短册三层独立运动，铃舌探出铃口下方，连接线重力弧垂
- 三只高低错落、轮廓分离的瓷铃，共用单张 SVG，每只独立铃舌；悬停命中区收敛到可见铃串
- 展板默认摆到铃铛左边，左边空间不足自动翻到右边

### 🐛 修复

- 远程缩放/分辨率/DPI 变化后风铃沿用旧坐标跑出可见区域：启动与运行中自动拉回当前屏幕，贴边保留 16px 余量
- 悬停声音循环断点：触发率提升保证声音叠放连续
- 动作清单字段名不一致、成功回包误判、长期会话超 256KB 目标判错、Hana 真实毫秒时间戳未识别；服务端强制重判兜底
- 透明窗口完整点击区，避免摆动角度间遮罩缺口

### 🔧 测试

- 风铃回归 56 项全绿（交互/声音/布局/断点防御/滤波回归），全套 87 项 JavaScript 测试通过

## v2.4.6 — 活跃窗口修复 + 面板体验优化（正式版）

- 修复：活跃窗口判定改为按「最后一条用户消息」时间，推送/助手回复不再干扰礼物去向
- 优化：送礼/互动后面板保持展开，可以连续操作
- 优化：互动按钮选中态更明显（之前选中色与背景几乎相同，按钮「隐身」）
- 优化：互动/送礼两个面板高度对齐，切换 tab 不再一会长一会短

## v2.4.5 — 礼物/互动推送带名字（正式版）

- 修复：送礼物的推送文案带上具体物品名（「🎁 给你带了东西：⭐星星许愿灯～」），助手一眼认出礼物，不再出现「送了礼物但助手没反应」的误会
- 互动、恶作剧的推送同样带上物品名
- check-visits 工具描述补充触发词，助手更容易主动查看待处理事件

## v2.4.4 — 安装包瘦身（正式版）

- 移除未使用的装饰素材文件（纸纹、和纸胶带、压花角、撕纸、缝线及两套未启用主题 CSS），安装包从 13.6MB 缩至约 200KB。
- 素材已备份存档，如需恢复随时可还原。
- 界面与功能无任何变化。

## v2.4.3 — 某处悄悄更新（正式版）

### 🔮 隐藏设定
某个隐藏设定有了部分更新，变得更懂事、更克制了。细节就不多说了，自己去发现吧。

### ⚡ 能量系统
- **实时扣减**：展板轮询同步扣当天工作消耗，打开展板就能看到精力随聊天下降（原来要等互动或次日重置才结算）
- **消耗系数调低 3 倍**：30 光粒 = 1 精力（原 10 光粒），通宵惩罚口径统一，高强度一天也只掉约 17 点
- **通宵惩罚按个人昨日光粒**：谁熬夜干活谁扣，躺平的不被连坐（上限 30）

### 🎀 头像框与装饰商店
- 8 款手绘头像框：星光/月亮/爱心/云朵/音符/蝴蝶结/花环/风车，每款专属动画，统一对角双贴纸布局
- 装饰商店升级为购买 + 衣橱二合一：切换任意助手、已拥有直接换上、正在用高亮
- 商店按类型分组排序，旧数据乱序/缺项/重复自动纠正

### ✏️ 顶栏编辑入口
- 弹窗隐藏/显示伙伴（隐藏只从展板消失，数据全保留）
- 「刷新列表」重新扫描找回所有伙伴，启动扫描不再覆盖隐藏标记

### 🛠 设置与兼容
- **删除误弹的 API Key 补填区**：选 Hana 已配置模型不再出现填 Key 框（原为字段名写错 + 无条件显示双重 bug），只有选「自定义 API」才显示；缺 Key 模型显示 ⚠️ 引导去 Hana 设置补
- **新增「反馈」按钮**：设置页直达 GitHub Issues
- **支持 OpenAI Responses 协议**：新版供应商（如 opencode-go）不再报“不支持的 API 协议”
- 页面响应加 Cache-Control no-store，修复 webview 缓存导致 UI 退回旧版的问题

### 🔐 安全与数据
- 助手 ID 白名单校验（防路径穿越/原型污染），购买/装备/卸载接口 target 校验
- saveData 备份时序修正（.bak 永远是上次成功内容）
- 页面背景改暖调浅薄荷绿，保持纸张感

### 🔧 测试
- 39 条自动测试全过（隐藏设定相关逻辑、能量扣减幂等、通宵惩罚边界等）

## v2.4.2 — 清晰版轻纸片手帐 UI（正式版）

- 降低纸纹、半透明背景和图片装饰的叠加，解决卡片边界发糊、页面显脏的问题。
- 统一伙伴墙、详情面板、统计卡、操作入口、礼物、纸条和弹窗的纯色纸面层级。
- 提高正文与辅助文字的对比度，补齐 focus、disabled、展开态和移动端布局。
- 仅调整前端视觉层，保留互动、送礼、充电、模型设置和数据结构。
- 正式包版本升级为 v2.4.2，数据目录保持不变，重启后继续读取原有数据。

## v2.4.1 — 轻纸片手帐 UI

- 将伙伴卡、统计卡、互动按钮、礼物卡、纸条和弹窗统一为轻纸片组件。
- 移除主要直线框，改用纸张层叠、轻微角度、柔和阴影与克制素材点缀。
- 保留现有功能与交互逻辑，移动端继续使用单列布局。

## v2.4.0 — 素材化手帐视觉

- 用 AI 合成的纸纹、和纸胶带、压花拼贴和缝线素材替换可见的内联 SVG 装饰。
- 保留原有伙伴墙、互动、送礼、拖拽排序和设置逻辑，只新增素材层与响应式视觉覆盖。
- 新增 `public/material-notebook.css`，统一管理暖纸色板、层次、装饰与 reduced-motion。

## v2.3.1 — 心情值改造：自主生活 + 锦上添花

### 🎨 心情系统重构
- **心情有来由了**：每日重置回顾昨天的活动推演心情走向（收到礼物→心情上浮，互动/充电→小确幸）
- **不依赖用户，锦上添花**：没人来时不惩罚、不责怪，助手走自己的日子（自主漂移 ±12），心情自然起伏、偶尔有人不太开心，但不是因为你没来
- **心情原因注入**：生成回应时会带上心情的来由（如「心情不错（昨天收到了一束花）」），拟人 agent 可以顺势演绎，不再被硬数值指挥
- **温和回归**：只在边缘拉回（很嗨才降温，很低才打气），长期分布：平稳约 6 成、不错约 3 成、偶尔有点闷约 1 成
- **恶作剧不降心情**：恶作剧是朋友间开玩笑，当天有小开心（玩闹性质），隔天没有负面余波
- **事件记录**：互动/礼物/恶作剧/充电都会记入当天活动，作为次日心情推演的依据（兼容旧数据，无记录时走自主漂移）

### 🐛 修复
- **拟人 agent 混乱问题**：所有注入 agent 的状态描述从硬数值（心情 60/100）改为模糊描述（心情平稳），消除与角色卡情绪打架的双轨冲突
- **注入文案统一**：generateReply、check-visits、sense-state 三处状态注入统一走公共函数，消除文案不一致

### 🔧 开发
- 新增自动测试 `tests/variables.test.js`（13 条）：覆盖心情推演、原因生成、描述边界、范围约束、事件记录
- 运行方式：`node --test tests/`

## v0.1.6 — 检查更新 + 一键下载

### ✨ 新功能
- **检查更新**：设置面板新增「📦 检查更新」按钮，自动请求 GitHub 对比版本
- **更新内容展示**：有新版时显示 GitHub Release 更新日志，用户看完再决定是否下载
- **一键下载**：直接下载最新版 zip，无需跳转网页

### 🎨 优化
- 设置面板按钮布局重新设计（保存/测试/更新分组排列，大小统一）

## v0.1.5 — 关机键崩溃演出

### 🎭 新功能：关机键崩溃演出
- 按关机键后，目标助手会自动表演「系统被关机」的视觉效果（乱码 ████ + 错误提示 + 吐槽 + 自然衔接继续正文）
- 演出指令通过 `session:send` 注入，一条回复内完成崩溃 → 吐槽 → 恢复的完整流程

### 🐛 修复
- **聊天框消失**：删除了 `injectSystemMessage` 函数，该函数往会话 JSONL 写入 `role: "system"` 消息导致 Hana UI 解析器崩溃
- **关机键不生效**：abort 调用改为 `await`，等生成真正停止后再注入演出指令，修复 `session_busy` 错误
- **标记已读死锁**：visit 记录创建移到 `session:send` 之前，确保助手调用 `check-visits` 时能找到待处理记录
- **LLM 配置拦截**：恶作剧类（关机/怪话）不再强制要求配置闲不住模型

### 🔧 优化
- autoReply 生成改为异步（不阻塞关机响应）
- 每个助手独立处理 pendingVisit，互不影响

## v0.1.1 — 修复与优化（2026-07）
- 模型设置：自动识别已配置供应商
- 首次自动选择默认模型

## v0.1.0 — 初始发布（2026-07）
- 展板：显示所有助手实时状态
- 互动：安静陪着、哼小调、便签画画、扇风
- 恶作剧：关机键、说怪话
- 小铺：光粒兑换礼物
- 光粒系统：自动积累 + 领取
- 隐藏设定：部分功能悄悄上线
