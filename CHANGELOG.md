# 闲不住 更新日志

## v2.3.0 — 零侵入推送 + bug 修复

### ✨ 新功能
- **与「在干嘛」弹幕插件联动**：每次互动、恶作剧、送礼时，如果弹幕开着，会自动发一条弹幕信息到桌面浮层
- **加密存储 API Key**：API Key 不再明文保存，改为本地 XOR + base64 混淆存储，前端显示「已加密」标识
- **新风格：脑洞袭击**：随机触发无厘头冷幽默弹幕，增加互动趣味性
- **版本检查**：自动检测 GitHub 最新 Release，有更新时提示
- **充值功能**：前端「⚡ 充电」按钮，调用对应助手的充值逻辑

### 🐛 修复
- **llm.js 大括号平衡修复**：修复了 `processVisitEvent` 函数缺少闭合大括号导致的模块加载失败问题
- **推送准确度优化**：完善 `findLatestSessionId` 解析逻辑，增加 `session-titles.json` 备选方案
- **身份协议零侵入**：彻底移除所有身份协议注入，改为纯工具调用方式
- **编码安全**：前端 Token 传递增加 XSS 防护（`\u003c` 转义）

### 🔧 优化
- 路由注册增加 `encryptKey`、`readBody`、`json` 等辅助函数
- 展板数据显示优化
- 跨轮重复抑制机制增强
- `escapeHtml` 包装增强 XSS 防护

---

## v0.1.6 — 检查更新 + 一键下载

### ✨ 新功能
- **检查更新**：设置面板新增「📦 检查更新」按钮，自动请求 GitHub 对比版本
- **更新内容展示**：有新版时显示 GitHub Release 更新日志，用户看完再决定是否下载
- **一键下载**：直接下载最新版 zip，无需跳转网页

### 🎨 优化
- 设置面板按钮布局重新设计（保存/测试/更新分组排列，大小统一）

## v0.1.5 — 关机键崩溃演出

### 🎭 新功能：关机键崩溃演出
- 按关机键后，目标助手会自动表演「系统被关机」的视觉效果（乱码 ████ + 错误提示 + 吐槽 + 自然衔接继续正文）
- 演出指令通过 `session:send` 注入，一条回复内完成崩溃 → 吐槽 → 恢复的完整流程

### 🐛 修复
- **聊天框消失**：删除了 `injectSystemMessage` 函数，该函数往会话 JSONL 写入 `role: "system"` 消息导致 Hana UI 解析器崩溃
- **关机键不生效**：abort 调用改为 `await`，等生成真正停止后再注入演出指令，修复 `session_busy` 错误
- **标记已读死锁**：visit 记录创建移到 `session:send` 之前，确保助手调用 `check-visits` 时能找到待处理记录
- **LLM 配置拦截**：恶作剧类（关机/怪话）不再强制要求配置闲不住模型

### 🔧 优化
- autoReply 生成改为异步（不阻塞关机响应）
- 每个助手独立处理 pendingVisit，互不影响

## v0.1.1 — 修复与优化（2026-07）
- 模型设置：自动识别已配置供应商
- 首次自动选择默认模型

## v0.1.0 — 初始发布（2026-07）
- 展板：显示所有助手实时状态
- 互动：安静陪着、哼小调、便签画画、扇风
- 恶作剧：关机键、说怪话
- 小铺：光粒兑换礼物
- 光粒系统：自动积累 + 领取
- 小纸条：助手互动后自动生成回应
