# 闲不住 更新日志

## v0.1.7 — 关机重启 + 纸条折叠 + 樱花光晕

### 🎭 关机键重做：崩溃演出 + autoReply 指令注入
- 按关机键后，插件同步生成崩溃表演文本（乱码 ████ + 吐槽），设为 autoReply
- abort 中断后发「重启！」，助手按协议调用 check-visits 拿到演出剧本并直接演绎
- 崩溃剧本由插件自己的 LLM 生成，适配各助手性格
- 不再依赖 context.system（对 agent session 不生效），整个流程更可靠

### 📝 小纸条弹窗重做：折叠式卡片设计
- 进入弹窗先看到所有助手概览，点击展开/收起查看各助手纸条
- 每个助手卡片含头像色块、名字、纸条数量标签
- 展开动画平滑滑入，支持同时展开多个助手
- 助手的颜色体现在每条纸条的左侧 3px 竖条上

### 🎨 小纸条按钮呼吸动画改进
- 光晕从白色改为樱花粉（#f472b6 → #f9a8d4），显眼且柔和
- 去掉了冗余的小红点，纯呼吸效果更干净
- 修复按钮首次点击不生效的 bug（overlay 变量未赋值）

### 🔧 优化
- `processVisitEvent` 跳过已有 autoReply 的 visit，不覆盖崩溃剧本
- identity.md 协议增加「重启！」关键词触发说明
- check-visits 指令强化：崩溃剧本直接用「回复正文（请完整使用）」标识

## v0.1.6 — 检查更新 + 一键下载

### ✨ 新功能
- **检查更新**：设置面板新增「📦 检查更新」按钮，自动请求 GitHub 对比版本
- **更新内容展示**：有新版时显示 GitHub Release 更新日志，用户看完再决定是否下载
- **一键下载**：直接下载最新版 zip，无需跳转网页

### 🎨 优化
- 设置面板按钮布局重新设计（保存/测试/更新分组排列，大小统一）

## v0.1.5 — 关机键崩溃演出

### 🎭 新功能：关机键崩溃演出
- 按关机键后，目标助手会自动表演「系统被关机」的视觉效果（乱码 ████ + 错误提示 + 吐槽 + 自然衔接继续正文）
- 演出指令通过 `session:send` 注入，一条回复内完成崩溃 → 吐槽 → 恢复的完整流程

### 🐛 修复
- **聊天框消失**：删除了 `injectSystemMessage` 函数，该函数往会话 JSONL 写入 `role: "system"` 消息导致 Hana UI 解析器崩溃
- **关机键不生效**：abort 调用改为 `await`，等生成真正停止后再注入演出指令，修复 `session_busy` 错误
- **标记已读死锁**：visit 记录创建移到 `session:send` 之前，确保助手调用 `check-visits` 时能找到待处理记录
- **LLM 配置拦截**：恶作剧类（关机/怪话）不再强制要求配置闲不住模型

### 🔧 优化
- autoReply 生成改为异步（不阻塞关机响应）
- 每个助手独立处理 pendingVisit，互不影响

## v0.1.1 — 修复与优化（2026-07）
- 模型设置：自动识别已配置供应商
- 首次自动选择默认模型

## v0.1.0 — 初始发布（2026-07）
- 展板：显示所有助手实时状态
- 互动：安静陪着、哼小调、便签画画、扇风
- 恶作剧：关机键、说怪话
- 小铺：光粒兑换礼物
- 光粒系统：自动积累 + 领取
- 小纸条：助手互动后自动生成回应
