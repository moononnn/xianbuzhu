# 闲不住 更新日志

## v2.4.0 — 补投递队列 + 锁范围收窄 + 超时链路贯通（工程审查第二阶段）

### 🐛 修复
- **补投递队列（信箱机制）**：互动/礼物/充电推送失败时，事件不再静默丢失——标记 `deliveryPending` 进入「信箱」，由 60 秒定时任务自动重投（复用 2s/5s/10s 重试），送达后清除标记；尝试 10 次仍失败则放弃重试（记录保留，可查）。此前推送失败后光粒已扣、事件已标记 completed，助手永远收不到
- **全局锁收窄**：/api/visit 只有 load-modify-save（读数据→扣光粒→建事件→写盘）在短锁内；模型生成、消息推送（含 2/5/10s 重试等待）全部移到锁外。此前一个请求等待繁忙会话时，所有其他互动请求都要排队
- **网络请求真正取消**：callLLM 支持外部取消信号，事件超时后旧任务的 HTTP 请求会被实际中止（此前只停止写盘，网络请求仍跑到自己的 30 秒超时）；generateReply/generateCrashReply/generateBrainrot/generateAndSaveNote 全部透传信号
- **双定时器清理**：事件超时逻辑的两个定时器都保留引用并在 race 结束后一并清理，不再残留 30 秒
- **生成失败不白扣光粒**：说怪话改为先生成再扣费（此前先扣光粒，生成失败时白扣 3 光粒）
- **.bak 备份失败告警**：备份复制失败不再静默忽略，写入警告日志（写入继续，但明确告知回退能力可能不可用）

### ✨ 改进
- **状态阈值单一事实源**：心情/精力档位（含 label、emoji、颜色档）统一定义在 lib/data.js，describeMood/describeEnergy、sense-state、展板前端全部由后端档位函数驱动，不再三处手写 80/65/40/25 与 70/40/20
- **助手 ID 校验统一**：校验规则移到 lib/validate.js 公共模块，伙伴扫描与接口校验用同一套规则；允许合法 Unicode 目录名（此前 ASCII 白名单会误伤中文助手 ID），仍拒绝路径分隔符、控制字符、`.`/`..` 与原型链特殊键
- **充电通知补投递**：充电效果已生效但通知未送达时，也会进入补投递队列
- **怪话失败明确提示**：说怪话推送失败时前端明确提示“没送达”（此前静默）

### 🔧 开发
- 新增 tests/delivery.test.js（投递状态迁移、接口级 pushed 真实性、补投递集成循环、超时不写盘、阈值边界）与 tests/llm-cancel.test.js（外部 signal 真正取消 fetch），更新 tests/fixes.test.js 的 ID 校验断言，全部测试共 38 条通过

### 🔍 交叉审查修复（发布前）
- **关机键失败不再滞留**：unplug 的 abort/注入失败时事件标记为已读（received），不再无限挂在前端待处理列表（此前助手只在收到「重启！」时才读它，注入失败则永远无人消费）
- **历史脏数据兑底**：loadData 时清理 partnerConfig 中的非法键（旧版本校验宽松时沉淀的），防止已保存配置绕过扫描过滤直接用于路径拼接
- **工具层校验统一**：update-status 的 partner 参数补 isValidAgentId 校验（非法值回退到调用方自己），与接口层同规则，防实例级原型污染
- **补投递节流**：visit 触发补投递带 30s 节流（定时器触发不受限），高频互动时不再反复无效扫描
- **recharge 锁纪律统一**：充电的数据段（检查→扣费→改体力→写盘）纳入短锁，与 visit 同一纪律（锁内无 await）
- **热重载兼容**：bus 引用改为取最新注册的 ctx，补投递定时器在重复注册时不会产生双定时器，旧闭包也能用新 bus
- **注释诚实化**：说怪话的“生成失败不扣光粒”表述修正（generateBrainrot 有备选文案兑底，实际不返回空，扣费与旧版一致）；给 75/38 回归边界补注释说明与展示档位的语义区别

---

## v2.3.9 — 工程审查修复（推送状态诚实 + 超时取消 + 输入加固）

### 🐛 修复
- **推送状态诚实化**：互动/礼物/充电推送改为等待真实结果再返回（原来不等待，界面显示"已发送"但助手可能没收到）。前端根据 pushed 字段区分"已送达"与"她暂时没在线"，与说怪话流程同一思路
- **事件超时真正取消**：30 秒超时后主动通知旧任务停止，写盘前检查超时失效即放弃，不再与队列下一条任务并发写盘（原 Promise.race 只跳过等待，旧任务仍在后台跑模型/写数据）
- **.bak 时序修正**：修正 v2.3.7 引入的错误顺序（先替换再备份导致 .bak 变成当前内容），改回替换前复制旧文件，.bak 重新成为"上一次成功写入的版本"
- **状态阈值统一**：sense-state 与展板前端对齐 lib/data.js 的 80/65/40/25（心情）和 70/40/20（精力），同一个数值不再有三套语义
- **输入验证加固**：/api/visit、/api/recharge、/api/update-narrative 的助手 ID 加白名单正则并拒绝 __proto__/constructor/prototype（防路径穿越与原型污染）
- **注释诚实化**：sanitizeUserName 不再声称能"防 prompt 注入"（限长去控制字符只能防脏输入）

### ✨ 补充
- 恢复 register 双保险导出（plugin runtime 按名称查找与 default import 都能找到）
- 补充 /api/current-agent（获取当前正在对话的助手）、/api/partner-order（伙伴排序存取）接口（后端已就绪，前端排序 UI 待接入）

### 🔧 开发
- 新增 tests/fixes.test.js：覆盖 .bak 时序、状态阈值边界、助手 ID 白名单、用户名清洗，全部测试共 25 条通过

---

## v2.3.8 — 推送重试 + 工作量统计修正

### 🐛 修复
- **推送失败重试**：助手正在流式输出时 session:send 会抛 session_busy，导致互动/礼物/充电推送静默丢失（界面显示成功但助手没收到）。现在会话忙时等待重试（2s/5s/10s，最多 3 次），非忙碌错误不重试
- **工作量统计不再限制会话数**：原实现只统计今天最近 5 个会话，活跃用户开第 6 个会话后工作量被低估（展板 LP 和精力消耗都不准）。现在统计今天全部会话，性能由 1 分钟缓存兑底

---

## v2.3.7 — 双人交叉审查修复

### 🐛 修复
- **工作量统计时区基准**：mtime 兜底从本地时区 0 点改为北京今天 0 点（会话文件名是 UTC 日期，非北京时区机器凌晨会话不再漏算）
- **visit 创建并发丢记录**：POST /api/visit 整体加串行锁，快速连点多个互动/礼物不再互相覆盖
- **事件处理队列超时**：单条事件处理最长 30 秒，超时跳过，不再堵死后续事件
- **变量审计日志时机**：before 快照移到所有修改之前（原来只反映工作消耗差值）
- **恶作剧心情不打折**：精力低时的心情增量 5 折不作用于恶作剧（与"恶作剧不降心情"承诺一致）
- **好感阶段 NaN 兜底**：非法值不再误显示"疏远"，兜底为"初识"
- **Anthropic 请求头分离**：不再同时塞 Authorization 和 x-api-key，避免代理路由错乱
- **.bak 备份时序**：先原子替换再备份，.bak 永远是上次成功写入的内容
- **删除死代码**：getRechargeAutoReply（充电流程实际不用 autoReply 文案）

### 🔧 开发
- 新增 NaN 兑底测试，共 18 条全过

---

## v2.3.6 — 交叉审查修复

### 🐛 修复
- **无模型时恶作剧变量更新被跳过**：模型未配置时 processVisitEvent 直接返回，导致恶作剧扣了光粒却不改变量、不记事件、不出小纸条。现在变量更新/事件记录照常执行，只有回应生成和小纸条文本生成依赖模型
- **sense-state 零数字**：返回内容移除 mood/affection/energy 数值字段，只保留关系阶段和自然语言描述（彻底兑现"注入零数字"承诺）
- **update-status 描述修正**：工具说明不再写"默认hanako"，与实际行为一致（默认写调用方自己）

### 📝 文档
- README：更正身份协议说明（v2.3 起完全不注入身份协议，不是"保留一行极简指令"）；更正每日心情说明（好感度不再影响随机范围，改为事件驱动+自主漂移）；sense-state 说明补充"不含数值"
- CHANGELOG："不惩罚"措辞明确边界（心情不惩罚，好感度仍缓慢自然衰减）
- 修正 data.js 精力重置注释（100 减通宵惩罚，非固定 80）

---

## v2.3.5 — 小纸条回归修复 + 细节修复

### 🐛 真 bug 修复
- **小纸条永不触发（回归）**：小纸条判断原本嵌在 `if (visit.status === "pending")` 分支里，但互动/礼物 visit 是 completed 状态，导致价格阶梯（≥150 必出）和 5% 概率全部失效。已把判断挪出 pending 分支并抽成纯函数 `shouldTriggerNote`，所有事件类型恢复正常

### 🔧 修复
- **/api/data 写盘**：装饰迁移之外不再每次 GET 都 saveData（changelog 承诺兑现）
- **凌晨工作量漏算**：scanWorkStats 补 mtime 兜底（会话文件名是 UTC 日期，北京 00:00-08:00 的新会话匹配不上文件名前缀）
- **关机键不再要求配置插件模型**：恶作剧类（关机键/说怪话）豁免模型检查，与 changelog 声明一致
- **并发丢更新防护**：processVisitEvent 加串行队列，多个事件不再并发 load-modify-save 竞争
- **本地 .gitignore**：补全排除规则（_backups、.hallmark 等）

### 🔧 开发
- 新增 shouldTriggerNote 回归测试（含价格阶梯与概率档位），共 17 条

---

## v2.3.4 — 代码健康度修复

### 🔧 修复
- **格式化重排**：index.js、llm.js 用 prettier 重排缩进，消除 processVisitEvent 等历史事故函数的括号结构脆弱性
- **统一统计口径**：/api/data 不再内联统计，统一走 scanWorkStats（带 1 分钟缓存），展板 LP 与工作消耗同源；统计结果变化时才写盘，前端轮询不再每 GET 都 saveData
- **移除 sqlite3 CLI 依赖**：findLatestSessionId 改为从最新会话文件内容提取 sess id（零依赖），不再依赖本机 sqlite3 命令
- **路径穿越防护**：/api/avatar 的 agentId 加白名单正则（仅字母数字下划线连字符）
- **模型列表 URL 归一化**：fetchCustomModels 兼容带 /v1 的 baseUrl，避免 /v1/v1/models
- **update-status 默认值**：不再写死 hanako，默认写调用方 agent
- **负好感阶段**：AFFECTION_STAGES 新增「疏远」档（-20~-1），与 describeAffection 口径一致
- **送礼回礼注释**：data.jar += 3 注明是回礼设计
- **弹幕模板键简化**：冗余三元分支合并

### 🔧 开发
- 新增 getAffectionStage 负好感测试（共 16 条）

---

## v2.3.3 — 好感度也模糊化：注入层零数字

### 🐛 修复
- **好感度数值移除**：buildMoodContext 注入文本不再带「好感度 98（亲密）」硬数值，改为纯描述（你们亲密无间 / 你们已经很亲近 / 你们正在慢慢熟悉 / 你们还不算熟 / 你们之间有点疏远）
  - 原因：角色卡同样会定义关系（恋人/青梅竹马/陌生人），数值注入会和角色设定打架，与心情值的双轨冲突同理
  - 至此注入 agent 的状态描述全部零数字：心情、精力、关系全部模糊化
- 新增测试：describeAffection 边界 + 注入文本零数字断言（共 15 条）

---

## v2.3.2 — 心情值改造：自主生活 + 锦上添花

### 🎨 心情系统重构
- **心情有来由了**：每日重置回顾昨天的活动推演心情走向（收到礼物→心情上浮，互动/充电→小确幸）
- **不依赖用户，锦上添花**：没人来时不降低心情、不责怪，助手走自己的日子（自主漂移 ±12），心情自然起伏、偶尔有人不太开心，但不是因为你没来（好感度仍会缓慢自然衰减，每天 -1）
- **心情原因注入**：生成回应时会带上心情的来由（如「心情不错（昨天收到了一束花）」），拟人 agent 可以顺势演绎，不再被硬数值指挥
- **温和回归**：只在边缘拉回（很嗨才降温，很低才打气），长期分布：平稳约 6 成、不错约 3 成、偶尔有点闷约 1 成
- **恶作剧不降心情**：恶作剧是朋友间开玩笑，当天有小开心（玩闹性质），隔天没有负面余波
- **事件记录**：互动/礼物/恶作剧/充电都会记入当天活动，作为次日心情推演的依据（兼容旧数据，无记录时走自主漂移）

### 🐛 修复
- **拟人 agent 混乱问题**：所有注入 agent 的状态描述从硬数值（心情 60/100）改为模糊描述（心情平稳），消除与角色卡情绪打架的双轨冲突
- **注入文案统一**：generateReply、check-visits、sense-state 三处状态注入统一走公共函数，消除文案不一致

### 🔧 开发
- 新增自动测试 `tests/variables.test.js`（13 条）：覆盖心情推演、原因生成、描述边界、范围约束、事件记录
- 运行方式：`node --test tests/`

---

## v2.3.0 — 与在干嘛弹幕插件联动

### ✨ 新增
- **与「在干嘛」弹幕插件联动**：每次互动、恶作剧、送礼时，如果弹幕浮层开着，会自动发一条弹幕信息到桌面。两个插件终于能互相串门啦！

### 🐛 修复
- **llm.js 大括号平衡修复**：修复了 processVisitEvent 函数缺少闭合大括号导致的模块加载失败问题
- **身份协议彻底零侵入**：移除旧版残留的所有身份协议注入逻辑

### 🔧 优化
- API Key 加密存储（encryptKey 函数）
- 路由辅助函数（readBody、json）代码结构优化
- XSS 防护增强

---

## v0.1.6 — 检查更新 + 一键下载

### ✨ 新功能
- **检查更新**：设置面板新增「📦 检查更新」按钮，自动请求 GitHub 对比版本
- **更新内容展示**：有新版时显示 GitHub Release 更新日志，用户看完再决定是否下载
- **一键下载**：直接下载最新版 zip，无需跳转网页

### 🎨 优化
- 设置面板按钮布局重新设计（保存/测试/更新分组排列，大小统一）

## v0.1.5 — 关机键崩溃演出

### 🎭 新功能：关机键崩溃演出
- 按关机键后，目标助手会自动表演「系统被关机」的视觉效果（乱码 ████ + 错误提示 + 吐槽 + 自然衔接继续正文）
- 演出指令通过 `session:send` 注入，一条回复内完成崩溃 → 吐槽 → 恢复的完整流程

### 🐛 修复
- **聊天框消失**：删除了 `injectSystemMessage` 函数，该函数往会话 JSONL 写入 `role: "system"` 消息导致 Hana UI 解析器崩溃
- **关机键不生效**：abort 调用改为 `await`，等生成真正停止后再注入演出指令，修复 `session_busy` 错误
- **标记已读死锁**：visit 记录创建移到 `session:send` 之前，确保助手调用 `check-visits` 时能找到待处理记录
- **LLM 配置拦截**：恶作剧类（关机/怪话）不再强制要求配置闲不住模型

### 🔧 优化
- autoReply 生成改为异步（不阻塞关机响应）
- 每个助手独立处理 pendingVisit，互不影响

## v0.1.1 — 修复与优化（2026-07）
- 模型设置：自动识别已配置供应商
- 首次自动选择默认模型

## v0.1.0 — 初始发布（2026-07）
- 展板：显示所有助手实时状态
- 互动：安静陪着、哼小调、便签画画、扇风
- 恶作剧：关机键、说怪话
- 小铺：光粒兑换礼物
- 光粒系统：自动积累 + 领取
- 小纸条：助手互动后自动生成回应
