# 测试说明

## JavaScript 回归测试

```powershell
# CI（Linux，Node 24）：并发跑，与 GitHub Actions 一致
node --test tests/*.test.js

# Windows 本地：建议串行（原因见下）
node --test --test-concurrency=1 tests/*.test.js
```

> ⚠️ 已知问题：node test runner 并发跑多个测试文件时，偶发
> `Unable to deserialize cloned data due to invalid or unsupported version` 失败
> （子进程 IPC 消息反序列化损坏，与测试内容无关——同一批文件串行必过）。
> 此问题在 Windows 和 Linux CI 上均偶发出现过，因此本地与 CI 统一用串行跑。

覆盖：互动/送礼/恶作剧业务、隐藏助手边界、并发写入、数据备份恢复、变量迁移与每日重置、主动心意有效期内的疏远衰减协调、伙伴刷新与心意节奏保留、会话选择（跨助手、长会话、真实毫秒时间戳）、风铃代理强制重判目标、风铃首次轮询按已读/未读及 deliveredAt 分流、心意送达幂等与“先收着”独立状态、多条心意队列与最新优先、助手 ID 白名单（路径穿越/原型污染/未登记/隐藏助手拒绝）、推送链路（bus 携带、总超时兜底、锁释放、sessionPath 目标不被 subagent 会话污染）、回礼匹配与一次性消费、互动/礼物/怪话回礼推送、关机键回礼的 check-visits 兜底、双层性格披露与心意节奏对频率/表达的联动、自动礼物/现场偏好与近三天冷却、心动计划、在线与静默两道闸（聊天/任务忙碌不拦截）、主动心意失败分类与 1/3/10 分钟退避、内容失败单轮预算、重试耗尽、卡住生成恢复、重试到点的离线门槛、原计划 ID 幂等、隐藏后旧计划取消与在线判断不读取隐藏助手、心意信箱展示、风铃读取已送达心意、已回应心意停止继续邀请、旧回礼状态迁移、标签和普通文本形式的模型外壳清洗、旧心意空正文的兼容兜底、声音指纹与 description/公开性格合并、真实四助手链路与同标签反压平、句式与标点偏好、主动心意审核的性格保真与结构去模板、强词/弱词/冲突词事件一致性、普通小纸条审核兼容、无模型配置时不落固定模板心意、融合窗口重叠面积/启动位置等纯函数、送达物理响铃（三次交替 kick 稳定三声、单 kick 从静止必响、送达窗口内非悬停可响、kick 间隔不被冷却吞掉）、鼠标点击铃舌冲量从静止快速响铃、心跳注入时钟贯穿到 checkedAt 与失败重试时间（不因测试跨进静默时段偶发漂移）。

## 风铃回归测试

```powershell
python -m unittest discover -s tests -p "test_fengling_*.py"
```

> 安全护栏：风铃回归测试启动时会把 `HANA_HOME` 指向临时目录，避免静音、拖动位置等用例改写真实运行状态；Windows 本地仍建议串行运行。

覆盖：分辨率或 DPI 缩放后旧坐标自动拉回、屏幕边缘保留可见余量、鼠标左右来向判断、光标速度到风力的平滑映射、入场推力方向、沿可见铃串轮廓判定悬停、透明四角不误触发、边缘进出滞回与离开宽限、弹窗无系统矩形阴影、切换互动/送礼保持锚点、工具窗焦点/应用失活收起、拖动风铃时弹窗保持并跟随、缓存先显示与后台刷新分流（含真实线程回传）、旧快照不误删刚到达的心意、相同动作数据不重复重建按钮、透明窗口完整点击区无采样缺口、最终尺寸下可见的柔线弧垂与摆速差动态变形、三只错落铃铛共用单张 SVG 且各有铃舌、铃舌/短册非刚性连接、铃舌椭圆边缘与铃口接触几何、碰壁反弹、一次悬停只允许一次响铃、冷却期间不消耗响铃机会、4.8 秒真实多铃实录与三个独立播放位置容量、旧声音开关迁移、四档音量持久化与原子写入、损坏主状态回退到最近一次成功备份、静音选择跨重启保留、风铃与融合球共用音量状态并可往返保存、融合球右键二级声音菜单、四档互斥勾选与二级菜单命中保护、主音量真实缩放 WAV、Qt 多声道重叠播放与 Windows 异步文件回退、三声送达提示复用独立播放槽、播放冷却/静音分支、左/右键职责分离、自动跟随目标、互动与送礼菜单结构、目标选择自动→助手→前 5 个活跃对话的单一路径、必须先选助手、固定 sessionPath 传递、外部点击自动收起且不反弹重开、请求期间防重复点击、面板纸面背景完整渲染、动作面板与右键菜单鼠标离开后自动半透明并移回恢复、隐藏后清理淡出状态、主动心意只播放提示音且不自动打开普通面板、用户点击后展示心意卡、主页面确认后的已读同步、送达后未读心意重启不重复响铃、多条心意队列保留最新优先、单条“先收着”持久化、打开时即时补取后端心意、新心意不重置已打开面板、送礼者/礼物/留言卡片、继续互动引导；融合版复用解语花朗读回复刷新入口、融合朗读窗实际入口类型与球/窗双向拖动相对位置；新增真实窗口拖速平滑/限幅、触边归零与反向、极短 flick 余势、铃身/短册/铃舌分层惯性、铃舌独立扭转投影、碰壁自转冲量、拖动反向相对风、短册结点可见横向偏移与铃舌持续滞后、花梗/花冠/叶片重量顺序、原版/融合版主球与朗读窗拖动共用采样，以及旧版解语花融合兼容兜底。

## 融合球回归测试

```powershell
python <本地开发目录>/悬浮球融合·风铃解语花/app/test_fusion_ball.py
```

覆盖：融合球 88×88 与场景缩放、两边正式面板宽度、风铃页复用自动/手动/按助手目标选择器、两页共用同一个朗读窗口、解语花页/朗读页左侧锚定、朗读/右键面板淡出、空白点击收起、右键菜单仅保留声音/关闭、右键菜单跟随融合球、Ask 常驻轮询与自动切到解语花页，以及离屏真实渲染和按压花瓣。

## 最近结果

- 2026-08-26：JavaScript 154 项、风铃/融合 Python 130 项通过；新增自推送/brainrot、旧格式/非法时间戳与第 60 个候选边界会话扫描回归、目标读取失败重试和固定目标标题回显；自动模式文案回归为“跟随最近活跃的对话”，正式风铃面板与融合球语法检查通过，融合球运行时复用同一目标选择实现，并覆盖动作回包不覆盖新目标。
- 2026-08-24：JavaScript 145 项通过；新增已回应心意保留到自然过期、回礼内容回显与旧关联兼容回归；改动文件语法检查和服务端渲染后的页面内联脚本检查通过
- 2026-08-24：JavaScript 145 项、风铃/融合 Python 122 项通过；新增拖动反向相对风目标、短册结点可见横向偏移、铃舌持续滞后与原版/融合版共用物理回归；保留 Qt 音频源预加载、Loading 阶段 winsound 回退、测试状态隔离，以及黄色铃舌放大、提前碰壁与鼠标点击冲量回归
- 2026-08-23：JavaScript 144 项、风铃/融合 Python 117 项通过；新增拖速采样、触边/反向/急停、铃舌扭转投影、外部面板带球拖动、花朵重量层与旧版融合兜底回归；最终尺寸状态表和真实计时拖动序列预览通过
- 2026-08-23：JavaScript 144 项、风铃 Python 108 项通过；新增送达物理响铃回归（三次交替 kick 稳定三声、单 kick 从静止必响、送达窗口内非悬停可响、kick 间隔不被冷却吞掉）
- 2026-08-22：JavaScript 132 项、风铃 Python 94 项通过；交付复查后补上公开性格合并、标点软偏好、强弱 cue/冲突词事件校验、真实四助手链路和普通小纸条兼容回归
- 2026-08-21：JavaScript 122 项、风铃 Python 89 项通过；3.0.41 收紧为“先助手、后前 5 个活跃对话”，原版/融合版共用顺序选择器
- 2026-08-20：JavaScript 112 项通过；风铃回归 77 项通过；融合球 Python 9 项通过（含 88×88、正式双面板、左侧锚定、朗读/右键淡出、空白收起和右键跟随回归）
- 2026-08-19：JavaScript 109 项通过（心意节奏、自动偏好、隐藏助手边界）；风铃回归 77 项通过
- 2026-08-19：JavaScript 98 项通过（取消主动心意忙碌门槛，在线聊天/运行任务时仍放行）；风铃回归 77 项通过
- 2026-08-19：JavaScript 98 项通过；风铃回归 77 项通过（回礼匹配、直接推送、关机键 check-visits 兜底、已回应心意提示、心意送达摆动、已读同步、独立提示音、音量持久化与弹窗淡出效果）
- 2026-08-18：JavaScript 89 项通过（串行，3.0.10 改动后）
- 2026-08-10：JavaScript 60 项通过
- 2026-08-18：风铃回归 65 项通过（3.0.6 改动后）
- 2026-08-10：风铃回归 58 项通过
