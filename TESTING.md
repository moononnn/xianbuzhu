# 测试说明

## JavaScript 回归测试

```powershell
# CI（Linux，Node 24）：并发跑，与 GitHub Actions 一致
node --test tests/*.test.js

# Windows 本地：建议串行（原因见下）
node --test --test-concurrency=1 tests/*.test.js
```

> ⚠️ Windows 本地已知问题：Node test runner 在 Windows 上并发跑较多测试文件时，偶发
> `Unable to deserialize cloned data due to invalid or unsupported version` 失败
> （子进程 IPC 消息反序列化损坏，与测试内容无关——同一批文件串行必过、Linux CI 必过）。
> 本地验证以串行为准；不要为此改测试内容，CI 保持并发。

覆盖：互动/送礼/恶作剧业务、并发写入、数据备份恢复、变量迁移与每日重置、伙伴刷新、会话选择（跨助手、长会话、真实毫秒时间戳）、风铃代理强制重判目标、助手 ID 白名单（performVisit 路径穿越/原型污染/未登记助手拒绝）、推送链路（bus 携带、总超时兜底、锁释放、sessionPath 目标不被 subagent 会话污染）。

## 风铃回归测试

```powershell
python -m unittest discover -s tests -p "test_fengling_*.py"
```

覆盖：分辨率或 DPI 缩放后旧坐标自动拉回、屏幕边缘保留可见余量、鼠标左右来向判断、光标速度到风力的平滑映射、入场推力方向、沿可见铃串轮廓判定悬停、透明四角不误触发、边缘进出滞回与离开宽限、弹窗无系统矩形阴影、切换互动/送礼保持锚点、工具窗焦点/应用失活收起、拖动风铃时弹窗保持并跟随、缓存先显示与后台刷新分流（含真实线程回传）、相同动作数据不重复重建按钮、透明窗口完整点击区无采样缺口、最终尺寸下可见的柔线弧垂与摆速差动态变形、三只错落铃铛共用单张 SVG 且各有铃舌、铃舌/短册非刚性连接、铃舌椭圆边缘与铃口接触几何、碰壁反弹、一次悬停只允许一次响铃、冷却期间不消耗响铃机会、4.8 秒真实多铃实录与三个独立播放位置容量、旧声音开关迁移、四档音量持久化、主音量真实缩放 WAV、Qt 多声道重叠播放与 Windows 异步文件回退、播放冷却/静音分支、左/右键职责分离、自动跟随目标、互动与送礼菜单结构、动作请求不携带手动助手选择、外部点击自动收起且不反弹重开、请求期间防重复点击、面板纸面背景完整渲染。

## 最近结果

- 2026-08-10：JavaScript 60 项通过
- 2026-08-10：风铃回归 58 项通过
